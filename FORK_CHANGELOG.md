# machamy/orca — 포크 변경 이력 (한국어 원본)

> English (secondary): [`FORK_CHANGELOG.en.md`](FORK_CHANGELOG.en.md)
> 이 문서가 원본입니다. 영어판은 이 문서를 옮긴 것입니다.

[stablyai/orca](https://github.com/stablyai/orca)를 적당히 따라가면서, 그 위에 내
변경을 **단계(rev)** 로 얹는 포크입니다. 원본 위에 무엇을 더했는지 단계별로 기록하고,
어느 빌드든 소스로 되짚을 수 있게 커밋 해시를 함께 남깁니다.

## 버전 표기

```
1.4.169-rc.0   .   machamy.1   .   local . <타임스탬프> . <커밋12자리>
└─ 원본 버전        └─ 내 단계     └─ 로컬 빌드 메타
```

- **원본 버전**: `package.json`의 값 (원본이 `release:` 커밋에서만 올림).
- **내 단계(`machamy.N`)**: 루트의 `FORK_VERSION` 파일 값. 포크 기능 마일스톤마다 올린다.
  원본을 새로 병합하면 원본 버전이 바뀌고, 내 단계는 이어서 센다.
- 커밋 해시는 파일명과 앱 리소스 양쪽에 박혀서, 설치된 빌드도 소스 추적이 된다.

---

## machamy.14 — 원본 `1.4.197` 기준 · 2026-09-25

원본을 **1,307커밋** 따라잡은 판. 포크 기능 추가는 없다. 원본 버전 번호는 그대로
`1.4.197` 이다(원본이 그 사이 `release:` 커밋을 내지 않았다). 기본 워크트리 전환 기능은
충돌 원인이 아니어서 그대로 뒀다 — 충돌 파일 11개에서 이 기능이 차지한 몫은 한두 줄뿐이었다.

### 마크다운 미리보기를 실제 GitHub 처럼 (machamy.13 미리보기 후속)
- **본문을 GitHub 스타일로 그린다.** 라이트는 흰 바탕 `#ffffff`·글자 `#1f2328`, 다크는 GitHub
  기본 다크 `#0d1117`·`#f0f6fc`. 본문 16px·줄 간격 1.5, h1·h2 밑줄, 인라인 코드 칩, `#f6f8fa`
  코드 블록, 테두리·줄무늬 표, 인용 왼쪽 막대, 굵은 구분선까지 GitHub 값을 그대로 쓴다.
  미리보기 본문에만 적용되고 리뷰 노트·검색·툴바는 Orca 모양 그대로다.
- **라이트 앱에서 인라인 코드가 안 보이던 문제.** 미리보기가 테마를 열 때 한 번만 읽어서,
  OS 가 다크인데 Orca 가 라이트로 뜬 순간에 열면 다크로 굳었다. 코드 칩 배경이 흰 바탕 위의
  흰색 10% 가 되어 사라지고 여백만 남아 글자가 띄엄띄엄 보였다. 원본에 이미 있는 반응형 테마
  훅(`useDocumentDarkTheme`)을 쓰게 했다.
- **글이 폭의 60% 에서 꺾이던 문제.** 원본의 새 리뷰 노트 기능이 **모든** 블록 오른쪽에 노트
  칸(220–300px)을 비워 둔다. 노트가 하나도 없어도 그래서, 여러 줄 문단은 좁게 꺾이고 한 줄짜리는
  끝까지 가서 들쭉날쭉했다. 노트나 작성 중인 칸이 있는 블록에만 칸을 두게 했다. 마우스를 올리면
  나오는 `+` 버튼은 그대로다.
- 인라인 `<code>` 에 `node="[object Object]"` 속성이 새던 것도 막았다.
- **GitHub 알림 블록.** `> [!NOTE]`·`[!TIP]`·`[!IMPORTANT]`·`[!WARNING]`·`[!CAUTION]` 이
  표식 글자가 그대로 보이는 인용문이었다. 이제 GitHub 처럼 색 막대·아이콘·제목이 붙은 알림으로
  그린다(색은 GitHub 값, 제목은 GitHub 처럼 영어).
- **줄바꿈을 GitHub 파일 보기처럼.** 미리보기가 원문의 줄바꿈 하나하나를 `<br>` 로 바꿨다(댓글
  방식). GitHub 은 `.md` 파일을 볼 때 그 줄들을 이어 붙인다. 이제 같게 잇는다 — 줄 끝 공백 두 칸이나
  `\` 로 쓴 명시적 줄바꿈은 그대로다. 사이드바 댓글 렌더링은 댓글이므로 그대로 둔다.
- 코드 블록 주석을 기울이지 않는다(GitHub 과 같게).
- **작업 목록·각주도 GitHub 처럼.** 체크박스가 항목에 따라 Orca 전용 상자와 기본 상자로
  섞여 나오던 것을 GitHub 과 같은 기본 체크박스(다크에선 다크 스타일)로 맞췄다. 각주는 GitHub 처럼
  구분선 아래 작고 흐린 글씨로, `↩` 는 이모지가 아닌 글자로 그린다. 알림 마지막 문단의 여백도 뺐다.
- **각주·앵커 링크가 이동하지 않던 문제.** 정제 단계가 id 앞에 `user-content-` 를 붙이는데
  미리보기는 정확히 같은 id 만 찾았다. 각주 링크(`#user-content-fn-1` → 실제 id 는 접두사가 두 번)
  와 원시 HTML id 로 가는 `#링크` 가 이동하지 않았다. GitHub 처럼 접두사 붙은 id 도 찾는다.

### 원본 병합 (1,307커밋, `1.4.197` 유지)
- 종류별: 수정 602 · **성능 224** · 기능 166 · 테스트 87 · 리팩터 64.
- 영역별: **모바일 208** · 네이티브 챗 97 · 릴레이 63 · 터미널 54 · 런타임 37 ·
  오케스트레이션 22 · 브라우저 21 · 에디터 20 · 에이전트 상태 19 · 사이드바 13.
- 눈에 띄는 원본 신기능: **ripgrep 번들**(로컬·WSL·SSH 검색), **Jupyter 노트북을 노트북처럼
  렌더하고 영속 커널로 실행**, 에이전트 세션 **전체 기록 검색**(`orca search`, 여러 컴퓨터),
  **기본 터미널 셸 선택**·셸 인자 설정, 터미널 **인라인 이미지**, diff 의 **변경 없는 구간 접기**,
  diff 댓글 **여러 줄 범위 선택**, 새 워크스페이스 작성기의 **기준 ref 선택**, 프로젝트별
  **gh 계정 고정**, 미리보기 탭 끄기 설정, ZCode·Muse Code·OpenCode 2 하니스.
- **원본이 Agent Map 을 통째로 없앴다**(#20929). 포크가 그 화면에 얹었던 워크스페이스 메뉴
  테스트는 대상이 사라져 계약 목록에서 뺐다. 같은 동작은 사이드바 메뉴로 계속 쓴다.

### 포크 쪽에서 손댄 것
충돌 25개를 풀었다. 원본이 또 파일을 파사드로 녹였고(`src/shared/rpc-contract/*`),
포크 수정은 새 모듈로 옮겨 심었다:

- **RPC 스키마.** `StrictOptionalBoolean`, `worktree.defaultSet` 선택자, 워크트리 폴더 필드를
  원본의 새 `rpc-contract` 모듈로 옮기고 파라미터 카탈로그를 재생성했다.
- **CLI.** 원본의 `terminal create --shell` 을 포크가 분리해 둔 `specs/terminal.ts` 로 옮겼다.
- **워크트리 ID 이전.** 원본이 추가한 행 재지정(닫힌 터미널 묘비, 클라이언트 호스팅 브라우저
  페이지)을 포크의 세션 이전 모듈에 합쳤다.
- **에이전트 상태 캐시.** 원본이 캐시를 읽기 전용으로 바꿔서, 워크트리 이전 때의 직접 쓰기를
  원본 입구(`admitLegacyAgentStatus`)로 돌렸다. 포크의 `effort` 필드와 원본의
  `modelSwitchCommand` 는 둘 다 살렸다.
- **줄 수 제한.** 원본이 키운 파일 5개가 포크 몫 때문에 `max-lines` 를 넘었다. 무효화 주석 없이
  포크 몫을 파일로 뺐다 — 포크 설정 타입(`fork-global-settings.ts`), 제목 기반 에이전트 판별
  (`worktree-title-agent-identity.ts`), 기본 전환 메뉴 항목, 유니티 시드 예약, 브라우저 URL
  동기화의 `worktreeId` 조회. 그 결과 `global-settings-types.ts`·`browser-page-pane.tsx` 는
  원본과 한 줄 빼고 같거나 완전히 같아져 다음 병합이 가벼워진다.
- **원본 새 테스트가 잡아낸 포크 버그 둘을 고쳤다.**
  - 유니티 시드가 워크트리를 만들자마자 워크트리 목록을 새로 읽었다. 그 스캔이 생성 경로의
    메타데이터 기록보다 먼저 떠서, 방금 만든 워크트리 이름이 옛 값으로 캐시될 수 있었다.
    이제 유니티 프로젝트일 때만(그리고 시드를 거절하지 않았을 때만) 목록을 읽는다.
  - 탭이 사라지는 문제를 쫓으려고 넣은 임시 진단이 **스토어가 바뀔 때마다 모든 터미널 탭을
    훑었다.** 워크스페이스가 많을수록 매 갱신이 느려지는 구조였다. 탭 목록이 실제로 바뀐
    갱신에서만 비교하도록 고쳤다.
- **원본 새 테스트 둘을 포크 동작에 맞췄다.** 원본은 `<details>` 를 편집하면
  `class="orca-details"` 를 붙여 저장하지만, 포크는 원문 여는 태그를 그대로 둔다
  (machamy.8 의 저장 시 변형 방지). 요약 안 이미지·수식 편집 테스트 2건의 기대값을 그쪽으로
  맞췄다.

### 검증
- 포크 계약 스위트 **120파일 / 1,517테스트** 통과. typecheck 0 오류, 변경 파일 lint 0 오류,
  `max-lines` 무효화 추가 없음.

## machamy.13 — 원본 `1.4.197` 기준 · 2026-09-25

코드 리뷰와 문서 읽기를 편하게 하는 판. 원본 병합은 없다.

### 마크다운 미리보기가 토글로 돌아왔고, 기본 보기가 됐다
- 리치 편집기는 HTML, 본문 속 제네릭(`List<Foo>`), 5만 자가 넘는 문서를 받지 않는다.
  그런 문서는 "HTML, JSX 또는 MDX가 포함되어 있으므로 코드 모드에서만 편집할 수 있습니다"로
  막혀 원문만 보였다. 원인은 렌더러가 없어서가 아니다 — 원본은 #849 에서 미리보기를 **별도
  탭**으로 옮기며 토글에서 뺐지만, 편집·diff 표면의 미리보기 렌더 경로는 그대로 남겨 뒀다.
- 그 경로를 토글에 다시 노출했다. 편집기 우상단 토글이 **소스 · 리치 · 미리보기 · 변경**이 된다.
  미리보기는 GitHub 과 같은 방식으로 그린다(GFM 표·체크박스, 원시 HTML, `<details>` 접기,
  코드 하이라이트, 문서 간 링크).
- **마크다운 파일은 기본으로 미리보기로 열린다.** 설정 → 편집기 → "마크다운 기본 보기"에서
  미리보기/리치/소스를 고른다. 파일별 토글은 여전히 우선한다.
- **diff 에서도 미리보기를 고를 수 있다**(변경된 쪽을 렌더). diff 의 기본값은 그대로 소스다 —
  diff 탭은 변경을 보려고 여는 곳이라 미리보기가 기본이면 변경이 가려진다.
- 참고: 본문에 백틱 없이 쓴 `List<MetaRewardDto>` 의 `<MetaRewardDto>` 는 GitHub 과 마찬가지로
  알 수 없는 태그로 취급돼 사라진다. 제네릭은 백틱으로 감싸면 보인다.

### 소스 제어 필터: 확장자로 거르기와 리뷰 프리셋
- 필터가 경로 부분 문자열이라 `.cs` 가 `Foo.cs.meta`·`Foo.csproj`·`style.css` 까지 끌고 왔고,
  여러 확장자를 한 번에 거를 방법이 없었다. 이제 점으로 시작하는 토큰(`.cs .json .md`,
  `*.cs`, `.cs.meta`)은 **경로 끝**과 맞추고 OR 로 묶는다. 점 없는 단어는 예전처럼 부분 문자열
  이라 폴더 검색은 그대로다. 작업 트리 변경과 기준 브랜치 대비 커밋 목록 양쪽에 똑같이 걸린다.
- 입력창 옆 **`.cs .json .md` 칩** — AI 가 짠 코드를 리뷰할 때 사람이 읽는 파일만 남긴다.
  다시 누르면 해제.

### 검증
- 포크 게이트 **120파일 / 1,468테스트** 통과, typecheck·lint 0.
- 렌더된 앱 E2E 2건: 7종 확장자 픽스처 필터링, 그리고 리치가 거부하는 문서(원시 HTML·본문 제네릭·
  5만 자 초과)가 기본 미리보기로 GitHub 식 렌더되고 토글로 소스↔미리보기를 오가는 것. 실제
  사내 문서(135KB)로도 한 번 렌더해 눈으로 확인했다.

## machamy.12 — 원본 `1.4.197` 기준 · 2026-09-09

machamy.11의 모델·에포트 배지가 패인마다 보였다 안 보였다 하던 것을 고친 판.
원인을 파고 보니 배지보다 **한 층 아래에 있던 결함**이었다.

### Orca가 Claude의 모델과 에포트를 버리고 있었다
- Claude 는 **모든 tool-use 훅**(PreToolUse·PostToolUse·Stop 등)에 지금 쓰는 모델과
  이번 턴에 적용된 추론 강도를 실어 보낸다 — `model: { id, display_name }` 와
  `effort: { level }` 이다(에포트는 `CLAUDE_EFFORT` 환경변수로도 노출된다).
- 그런데 공유 정규화기는 **문자열 필드만 남긴다**:
  `if (typeof value !== 'string') return undefined`. Claude 가 보내는 두 값은 객체라
  통째로 버려졌고, `buildClaudeStatusPayload` 는 애초에 둘을 넘기지도 않았다.
  결과적으로 **Claude 패인의 상태 행에는 모델이 한 번도 담긴 적이 없다.**
- 그래서 machamy.11 배지는 유일하게 남은 출처인 **시작 프레임 스크레이핑**에 의존했고,
  그 프레임은 세션 시작 때 한 번 그려지고 다시 안 그려진다 — 스크롤로 밀려나면 사라지고
  (배지가 빈다), 중간에 `/model` 로 바꾸면 **틀린 값이 남는다.**

이제 훅에서 두 값을 꺼내 상태 행에 담는다(`claude-model-effort-fields.ts`).
모델은 dated id 대신 `display_name` 을 쓴다 — 사용자가 `/model` 에서 고른 그 이름이다.

### 배지 우선순위 교정
- **모델·에포트 모두 상태 행이 1순위**가 됐다. 매 tool-use 훅마다 갱신되므로 중간에
  `/model`·`/effort` 를 바꿔도 다음 툴 호출에서 따라간다.
- 시작 프레임은 **fallback 으로 강등**했다. 게다가 프레임의 에포트는 **프레임이 지목한
  모델이 지금 표시할 모델과 같을 때만** 쓴다 — 모델이 바뀐 뒤의 프레임 에포트는 그
  모델의 값이 아니므로, 붙이면 에이전트가 쓰지 않는 조합을 사실처럼 그리게 된다.
- 곁가지 이득: 상태 행에 모델이 담기기 시작했으므로, 그 행을 읽는 다른 화면들도 Claude
  세션의 모델을 알게 된다.

### 유니티 자동 시딩이 machamy.9 이후 죽어 있었다
- 워크트리를 만들면 기본 체크아웃의 `Library` 를 CoW 클론하는 자동 시딩 호출이 원래
  `orca-runtime.ts:24937` 에 있었다. machamy.9 병합에서 원본이 그 파일을 40,904줄→58줄로
  쪼갰고, **호출 지점이 함께 사라졌다.** 시더 함수와 그 테스트는 살아남아서 게이트는 계속
  초록이었다 — 아무도 부르지 않는 함수가 통과하는 테스트를 갖고 있었던 셈이다. 그 뒤로
  새 유니티 워크트리는 우클릭 메뉴로만 시딩됐다.
- 원래 자리(`orca-runtime-create-managed-worktree.ts`, 로컬 워크트리 materialize 직후)에
  다시 꽂았다. 로직은 `runtime-local-worktree-unity-seed.ts` 로 빼서 원본 파일엔 호출
  한 줄만 남기고, **호출 지점 자체를 소스 인구조사로 고정하는 테스트**를 붙였다 — 다음
  병합이 또 삼키면 이번엔 게이트가 붉어진다.

### 웹뷰 서버 `node_modules` — 포크 코드 없이 해결
- 워크트리마다 `web/node_modules`(약 419MB)가 빠져 `npm install` 을 다시 돌려야 했다.
  조사해 보니 원본에 이미 답이 있다: 리포 루트의 **`.worktreeinclude`** 에 gitignore된 경로를
  적으면 새 워크트리마다 복사되고, macOS 에선 **APFS CoW 클론**으로 몇 초에 끝나며 디스크
  블록을 공유한다(`materializeWorktreePaths(..., 'copy')` → `cloneWorktreePathWithApfs`).
  존재하지 않는 경로는 조용히 건너뛰므로 웹뷰 서버가 없는 프로젝트엔 아무 영향이 없다.
- 그래서 포크엔 아무것도 더하지 않았다. 프로젝트 쪽에 한 줄이면 된다:
  ```
  web/node_modules
  ```

### 검증
- 신규 유닛 3파일(필드 판독 12건 + 수집 통합 3건 + 시딩 호출 지점 5건)을 포크 게이트에
  등록해 **117파일 / 1,445테스트** 통과. 런타임 통합 스위트 1,259건도 통과. typecheck 0, lint 0.
- 수집 통합 테스트는 실제 훅 페이로드를 흘려 **중간 모델 교체가 따라잡히는지**까지 본다.
- E2E 2건을 렌더된 앱에서 재실행 — 배지가 상태 행에서 `Opus · High` 로 뜨고, 우클릭
  메뉴로 위치가 바뀌고, 세로 분할에서 두 패인이 각자 경계 안에 배지를 갖는다.
- machamy.11 이 잘못된 우선순위를 "옳다"고 못박아 둔 테스트가 있었다. 뒤집었다.

## machamy.11 — 원본 `1.4.197` 기준 · 2026-09-07

원본 병합은 없다. 포크 기능 하나만 얹은 판 — **에이전트 패인마다 지금 쓰는 모델과
추론 강도(effort)를 모서리에 띄운다.**

### 모델·에포트 배지
- **에이전트가 도는 패인마다** 모서리에 `Opus · High` 같은 알약이 뜬다. Claude·Codex 둘 다.
  탭이 아니라 **패인** 단위라, 스플릿으로 Claude 와 Codex 를 나란히 띄워도 각각 자기 것을 단다.
- **알약을 좌클릭·우클릭하면 그 자리에서 메뉴**가 열린다 — 좌상단·우상단·우하단으로 옮기거나
  숨긴다. 숨기면 누를 대상이 사라지므로 **다시 켜는 것은 설정 → 실험 기능**이 맡는다(위치도 거기서 고른다).
- 기본값은 **우하단**이다. 패인 위쪽 가장자리는 제목과 분할·닫기 버튼이 이미 쓰고 있어서,
  위쪽 모서리는 사용자가 손을 뻗는 자리와 겹친다.

### 무엇을 근거로 그 값을 말하는가
세 출처를 강한 순으로 쓴다. 서로 증명력이 다르기 때문이다.
1. **Claude 의 TUI 헤더 프레임**을 읽는다 — 에포트까지 나오는 유일한 출처다(원본이 이미 갖고 있던 파서를 그대로 쓴다).
2. **에이전트가 보고한 상태 행**(`AgentStatusEntry.model`) — 모델만 나온다.
3. **영속된 런치 지정값** — Orca 가 무엇으로 띄웠는지일 뿐, 지금 무엇이 도는지의 증거가 아니다.

3번에서 온 값이 화면에 섞이면 알약을 **흐리고 기울임꼴**로 그리고, 툴팁이 "에이전트가 확인해
주지 않았다"고 말한다. **Codex 는 `/model` 을 자기 피커 안에서 처리해서 Orca 가 읽어올 수
없으므로**, Codex 생 TUI 패인은 대개 여기 해당한다. 확인 못 한 값을 사실처럼 그리는 것은
사용자가 검증할 수 없는 거짓말이라, 모델은 확인됐어도 에포트가 추정이면 알약 전체를 내린다.

### 검증
- 유닛 20건 신규(3파일). 포크 계약 게이트에 등록해 **116파일 / 1,426테스트** 통과.
  typecheck(node·web·cli) 0 오류, lint 0 오류.
- 렌더된 앱에서 Playwright E2E 2건: 배지가 자기 패인 안에 렌더되고, 우클릭으로 메뉴가 열리고,
  "좌상단"을 고르면 설정이 실제로 바뀐다. 그리고 **세로 분할에서 두 패인이 각자 배지를 갖고
  각자 경계 안에 있는지 좌표로 측정한다** — `.pane` 에 `position: relative` CSS 규칙이 없어서
  분할 시 배지가 탭 전체 기준으로 튈까 의심했는데, 재보니 아니었다. 그 사실을 테스트로 고정했다.

## machamy.10 — 원본 `1.4.197` 기준 · 2026-09-07

원본을 **561커밋** 따라잡은 판. 포크 기능 추가는 없다. machamy.9 와 달리 **병합이 삼킨
포크 기능이 0건**이었는데, 원본의 대규모 모듈 분해가 machamy.9 때 이미 지나갔기 때문이다.
이번 원본 묶음은 새 기능보다 **수정(237) 과 성능(136)** 이 대부분이다.

### 원본 병합 (561커밋, `1.4.178-rc.2` → `1.4.197`)
- 영역별 커밋 수: **SSH 43** · 네이티브 챗 32 · **릴레이 23** · 터미널 19 · 렌더러 18 ·
  클라우드 14 · Windows 13 · **모바일 10** · 워크트리 9 · 사이드바 9 · 오케스트레이션 9 · i18n 9.
- 성능 커밋만 136건이다(렌더러 16 · 터미널 10 · 영속화 7 · 워크트리 9 · 사이드바 5 ·
  시작 4 · 에디터 4). 원본이 반복 정렬을 잡는 린트 규칙과 성능 회귀 계약까지 같이 들여왔다.
- 눈에 띄는 원본 신기능: 구조화 네이티브 Claude 챗이 **Claude Agent SDK 위로 옮겨가며
  macOS·Linux 에서 켜졌고**, 모바일 **실제 백그라운드 푸시 알림**, 다중 에이전트 워크플로
  내구성(#16904), 진단 뷰어의 **공백 표시 토글**, 그리고 **한국어·프랑스어·일본어** 로케일
  확장(Orca 계정 설정·온보딩)이 들어왔다.
- 모바일·릴레이 수정 33건이 추가로 들어왔다. 폰이 데스크톱 로그아웃을 통보받는 경로,
  릴레이 셀 크래시율 알림, NAT 포트 동적 할당 등.

### 포크 쪽에서 손댄 것
충돌 12개를 풀었고, 포크 계약 게이트는 **한 건도 실패하지 않았다**. 포크 동작 손실이
없었다는 뜻이다. 원본 변화에 맞춰 따라간 것만 남았다:

- **유니티 프로세스 조회 재배선.** 원본이 프로세스 테이블 리더를 옮겨서
  `unity-editor-process-lookup.ts` 가 `shared/process-table-snapshot-reader` 를 보도록 고쳤다.
  Windows 리더는 읽기 전용 뷰를 돌려주므로 그쪽만 복사해서 넘긴다.
- **`visible-worktrees.ts` 분할.** 원본이 이 파일에 코드를 더하면서 `max-lines` 를 넘겼다.
  무효화 주석 대신 실제 seam 으로 쪼갰다 — 게시된 캐시(`visible-worktree-publication.ts`)와
  런타임 조회(`visible-worktree-runtime-lookups.ts`). 재수출로 import 지점은 그대로다.
- **원본 새 인구조사·픽스처에 포크 파일 등록.** 브라우저 탭 닫기 인구조사에 machamy.9 의
  테스트 스텁을 올렸고, 원본의 새 크로미움 오류 페이지 픽스처에 포크가 요구하는
  `worktreeId` 를 채웠다.

### 원본이 자기 픽스처로 깨뜨린 테스트 하나
- `orchestration-cli-subprocess` 가 붉게 떴다. 처음엔 병합 회귀로 봤는데, 원본 `main` 을
  그대로 세워도 똑같이 깨진다 — #16904 이 들여온 `stable_pane_required` 가드와 원본 자기
  픽스처가 어긋난 것이다. 원본 CI 는 `out/cli` 를 안 만들어서 이 파일을 건너뛰기 때문에
  드러나지 않는다. 픽스처에 라이브 페인과 Run 바인딩을 채워 고쳤고, **제품 코드는 손대지
  않았으며** 단정문도 그대로다.

### 검증
- 포크 계약 스위트 **113파일 / 1,406테스트** 통과. 사이드바 계열 321파일 / 2,735테스트 통과.
  typecheck(node·web·cli) 0 오류, lint 0 오류, `max-lines` 무효화 추가 없음.
- 전체 스위트를 분류한 결과 **포크가 원인인 회귀는 0건**이었다.
- 진단 노트 두 가지: 병합이 `package.json`·락파일을 바꾸므로 재설치 전 결과는 옛 라이브러리
  기준이라 못 믿는다. 그리고 `patched-dependencies-frozen-install` 을 직접 돌리면 npm 으로
  떨어져 `node_modules` 의 패치가 벗겨진다 — `pnpm install --frozen-lockfile` 로 복구한다.
- `cross-version-agent-session-wire` 는 병합이 미커밋인 동안 `HEAD` 를 읽어서 붉었다.
  커밋 후 13테스트 전부 통과 — 코드 문제가 아니라 실행 순서 문제였다.

## machamy.9 — 원본 `1.4.178-rc.2` 기준 · 2026-09-03

원본을 **912커밋** 따라잡은 판. 포크 기능 추가는 없고, machamy.8에서 드러난 유니티
단축키·창 포커스 문제 두 가지를 고쳤다.

### 유니티 단축키 변경
- **`⌘⌥U` = Open in Unity, `⌘⌥⇧R` = Open in Rider.** machamy.8 의 `⌃⌥U`/`⌃⌥R` 은 두 가지
  이유로 폐기했다 — `⌃⌥U` 는 Rectangle 같은 창 관리 앱의 "좌상단 1/4 타일링" 기본값이라
  유니티 대신 **창이 줄어들었고**, 대안으로 검토한 `⌥U` 는 macOS 데드키(¨)라 **터미널에
  문자가 들어갔다**(`neverInTerminal` 은 단축키 발동만 막지 문자 입력은 못 막는다).
  `⌘` 는 텍스트 입력을 억제하고 Rectangle 은 `⌃⌥` 계열만 쓰므로 둘 다 피한다. Rider 에
  `⇧` 가 붙은 건 `⌘⌥R` 이 이미 워크스페이스 이름변경이라 뺏지 않기 위해서다.
  설정 → 단축키에서 바꿀 수 있다.

### 유니티 재포커스: Hub 를 에디터로 오인하던 것
- 프로젝트를 Hub 에서 열면 **Hub 프로세스도 같은 `-projectPath` 를 달고 있어서**, "이미
  열려 있음" 판정이 에디터가 아니라 Hub 의 pid 를 잡을 수 있었다. 그러면 Hub 창이
  올라오거나 실패 팝업에 엉뚱한 pid 가 찍힌다. 원인은 `/Applications/Unity Hub.app/…`
  경로의 **공백**이었다 — argv[0] 을 첫 공백에서 자르면 `/Applications/Unity`, 파일명
  `Unity` 가 되어 에디터 판정을 통과했다. 이제 argv[0] 을 `-projectPath` 값과 같은 규칙으로
  파싱하고, 실행 파일이 정확히 `Unity`(`Unity.exe`) 인 프로세스만 에디터로 친다.
  시딩 가드는 그대로다 — Hub 가 프로젝트를 물고 있으면 캐시 복사를 안 하는 게 맞다.

### 원본 병합 (912커밋)
- 영역별 커밋 수: 터미널 49 · 렌더러 43 · **모바일 31** · 네이티브 챗 22 · 워크트리 20 ·
  git 19 · 브라우저 18 · Windows 17 · WSL 14 · 런타임 14 · SSH 13 · Codex 13 · **릴레이 12**.
- 모바일·릴레이 수정이 26건 들어왔다. 세션 복원 후 동기화 복구, 호스트 따라가기 탭
  스냅샷, 에이전트에 보낸 뒤 키보드 내리기, 워크트리 생성 시트가 PR 고르고 죽던 것,
  데몬이 죽었을 때 PTY 회수, 재시작 카운터 대신 릴레이 세대를 실은 PTY id 등. 폰에서
  자주 뜨던 오류 화면이 이 묶음으로 줄어드는지는 **확인 대상**이다.
- 원본이 큰 파일들을 편집한 게 아니라 **모듈로 쪼개고 얇은 파사드만 남겼다**
  (`orca-runtime.ts` 40,904→58줄, `ipc/pty.ts` 8,134→39줄, `useIpcEvents.ts` 4,539→7줄 등).
  그래서 그 파일들에 얹혀 있던 포크 코드는 갈 곳을 잃었고, 원본의 새 모듈들로 재이식했다.

### 병합이 삼킨 포크 기능 (포크 게이트가 전부 잡아냄)
충돌 55개를 푼 뒤 게이트가 18건의 실패로 손실을 드러냈다. 타입 검사로는 안 잡히는
것들이라, 계약 테스트가 없었으면 조용히 사라졌을 것이다.

- 절전·하이버네이션 kill 의 `retainSurface` 플래그 2곳 (`keepHistory` 와 분리된 플래그라는
  게 요점인데 원본 모듈엔 `keepHistory` 만 남아 있었다)
- `clearDeadLeafPtyBindings` 액션 통째로
- cold-restore 재개 클레임 처리 전체 — 부수로, 그 옆 테스트 2개가 "건너뛸 게 없어서"
  공허하게 통과 중이던 것도 드러났다
- follow 모드 웨이크 배관(스토어 액션 2개 + 호출부 200줄), 기본 전환 런타임 배관
- 기본 워크트리 전환의 우클릭 진입점, 유니티 메뉴와 확인 다이얼로그
- 포크 IPC 리스너 등록, `worktrees:changed` 의 `migrations`/`shieldOnly` 페이로드
- preload 리스너 2종, `RepoUpdate` 의 포크 필드 6개
- 경로 기준 정책 헬퍼 2개(기본 전환 후 git 의 main-worktree 플래그가 밀려난 쪽을 따라가서
  포크가 경로 기준으로 고쳤던 것)

### 원본이 잡아준 포크 결함
- machamy.8 이 만든 유니티 파일 3개가 `node:child_process` 를 직접 import 하고 있었다.
  원본이 이 위반을 감시하는 래칫 테스트를 들여오면서 드러났고, 공유 래퍼
  (`runProcess`/`spawnProcess`) 경유로 고쳤다 — Windows 에서 `windowsHide` 고정,
  `shell: true` 거부, `.cmd`/`.bat` 인자 인코딩을 받는다.

### 검증
- 포크 계약 스위트 **113파일 / 1,403테스트** 통과. typecheck(node·web·cli) 0 오류,
  lint 0 오류, `max-lines` 무효화 추가 없음(6개 파일은 실제 seam 으로 분리).
- 브라우저 마크다운 인계 E2E 를 렌더된 앱에서 재실행해 통과.
- 병합이 `package.json` 127줄·락파일 2,302줄을 바꿔서, 의존성 재설치 전까지 xterm 이
  옛 버전인 채 테스트가 돌고 있었다. 재설치 후 IME 테스트 23건의 실패가 사라졌다 —
  코드 회귀가 아니라 설치 상태 문제였다.

## machamy.8 — 원본 `1.4.178-rc.2` 기준 · 2026-08-28

문서와 유니티를 "여는" 동작을 정리한 판. 마크다운이 브라우저에서 원문으로 뜨던 것이
에디터의 서식 있는 뷰로 열리고, `<details>` 접기가 있는 문서도 리치 모드를 유지하며,
이미 떠 있는 유니티는 새로 켜는 대신 그 창을 앞으로 가져오고, 유니티/라이더를 단축키로
연다. 워크트리 폴더 기능은 코드만 실리고 이번 판에서는 접근 불가로 잠겨 있다.

### 브라우저 마크다운 → 에디터 인계
- 내장 브라우저로 `.md`/`.mdx`/`.markdown`/`.ipynb` 를 열면(주소창, 파일 탐색기의
  "Open in Orca Browser", 탭 복원, 재시도 — 모든 진입 경로) Chromium 원문 대신
  에디터의 서식 있는 뷰로 열린다. 게스트에 URL 을 싣는 모든 지점이 하나의 초크포인트를
  지나며, 다섯 번째 우회로가 생기면 테스트가 깨진다.
- 파일 탐색기에서 연 마크다운은 브라우저 탭을 아예 만들지 않는다 — 빈 "New Tab" 이
  남지 않는다. 권한 거부·stat 실패면 종전대로 브라우저에 원문으로 싣는다(무동작 없음).
- 비동기 검사 중 다른 곳으로 이동하거나(주소창·게스트 링크·SPA·에이전트 CDP 이동 전부)
  워크스페이스를 전환하면 늦게 끝난 검사가 끼어들지 않는다. 원격 워크스페이스는 파일
  단위 소유자 판정으로 건드리지 않는다.
- 에이전트가 `browser.goto` 로 요청한 raw 파일은 의도적으로 그대로 준다.
- **HTML/MDX 가 섞인 마크다운도 렌더된 상태로 열린다.** 리치 편집기가 그 문법을 지원하지
  못하는 문서는 소스 대신 마크다운 프리뷰(살균 HTML 렌더)로 착지하고, 편집 탭이 함께
  열려 소스는 한 클릭 거리다.
- **이전 동작으로 돌아가는 스위치가 있다.** 설정 → 실험 기능의 "브라우저 마크다운 인계"
  를 끄면 브라우저가 종전처럼 원문을 싣는다(.ipynb 인계는 포크 이전부터 있던 동작이라
  꺼지지 않는다). 기본값은 켬.

### `<details>` 문서의 리치 모드
- 접기(`<details>`/`<summary>`)만 있는 문서가 더는 "코드 모드 전용"으로 떨어지지 않는다.
  원인은 저장 시 여는 태그에 클래스가 강제로 찍혀 원문 보존 검사가 실패하던 것 —
  에디터에서 의미를 바꾸지 않았으면 여는 태그를 원문 그대로 돌려준다.
- 저장 손상 네 계열을 함께 수리: 코드 펜스 안의 리터럴 태그가 벗겨지던 것, 인라인 코드
  속 `</details>` 를 진짜 닫는 태그로 오인하던 것(백틱 런 길이 정확 일치, 여러 줄 스팬,
  블록 경계 차단), 4칸 들여쓴 코드 블록, 제목·수평선을 넘는 스팬. 원문 보존 검사
  자체는 손대지 않았다 — 통과하게 만든 것이 아니라 실제로 보존되게 만들었다.

### 유니티: 이미 열려 있으면 그 창을 앞으로
- "Open in Unity" 가 같은 프로젝트의 에디터가 이미 떠 있으면 두 번째 유니티를 띄우지
  않고 그 창을 앞으로 가져온다. 판정은 에디터 프로세스만 본다 — 같은 `-projectPath` 를
  달고 도는 `-batchMode` AssetImportWorker 는 "열려 있음"이 아니다. 경로 비교는 플랫폼
  추측이 아니라 경로 구문 기준(Windows 는 구분자·대소문자 접기, POSIX 는 구분).
- 창 올리기는 플랫폼별(macOS: System Events + 최소화 해제·raise, Linux: xdotool,
  Windows: SetForegroundWindow). 실패하면 원인별 문구에 **그 유니티의 pid** 를 실어
  알린다 — 접근성 거부와 자동화 거부를 구분해 맞는 설정 창을 안내하고, xdotool 미설치·
  창 없음은 권한 탓으로 돌리지 않는다. 방금 띄워 아직 창이 없는 경우는 "시작 중"으로
  말하고 pid 를 지어내지 않는다.
- 같은 프로젝트 동시 열기는 직렬화되고, 실행 직후의 재클릭은 락파일·유예 기준으로
  중복 실행을 막되 정상 종료 후에는 바로 다시 열 수 있다. 실행 직전 시딩 재확인으로
  살아 있는 `Library` 를 시더와 다투는 경합도 막았다.

### 유니티/라이더 단축키
- `⌘⌥U` = Open in Unity, `⌘⌥⇧R` = Open in Rider (Rider 기본 바인딩은 macOS 만 — 탐색이
  macOS 전용이라 다른 플랫폼에선 키만 삼키게 된다). 설정 → 단축키에서 재바인딩.
  Ctrl+Option 계열은 Rectangle 같은 창 관리 앱의 타일링 기본값과 겹치고, Option 단독은
  macOS 에서 데드키(¨·®)라 터미널에 문자가 들어간다 — Mod 는 텍스트 입력을 억제한다.
- 대상은 현재 워크트리. 메뉴 버튼과 동작이 같다 — 캐시 복사 제안 다이얼로그까지 같은
  경로를 탄다. 유니티를 쓸 수 없는 워크스페이스(SSH·런타임·폴더 워크스페이스·비 유니티)
  에선 조용히 아무 일도 없다. 터미널 포커스 중엔 절대 잡지 않고(AltGr 보호), 플러그인이
  같은 키를 잡았으면 양보하며, 삭제 중인 워크트리에는 실행하지 않는다.

### 워크트리 폴더 — 실려 있지만 잠김
- 사이드바에서 워크트리를 이름 있는 폴더로 묶는 기능이 코드로는 완성돼 실려 있다
  (렌더링·생성·이름변경·중첩·삭제·워크트리→폴더 전환·구버전 host 보호까지). 그러나
  이번 판에서는 **켤 방법이 없다** — 실험 설정 행 자체를 렌더하지 않는다. 기능이 없을 때
  사이드바 행 출력이 바이트 단위로 동일함을 테스트가 잠근다. 다음 판에서 실험 기능
  토글(기본 꺼짐)로 공개 예정.

### 검증
- 포크 계약 스위트가 956 → **1,298 테스트**로 늘었고(계약 파일 109개), 계약 목록의
  파수꾼 테스트(등록된 파일이 전부 실재하는지)가 이번에야 게이트에 들어갔다 — 그전엔
  목록이 조용히 줄어도 잡지 못했다.
- 두 벤더 교차검증 8회에서 실결함 약 35건을 재현→수정→테스트 고정으로 처리했다.
  저장 손상 계열 4건, 경합 6건, 잘못된 권한 안내, 플러그인 단축키 가로채기 포함.

## machamy.7 — 원본 `1.4.178-rc.2` 기준 · 2026-08-23

유니티 워크트리 색을 "알아서 정해지는 것"에서 **직접 고르고, 어디에 보일지 정하는 것**으로
바꾼 판. 색을 손으로 지정할 수 있고, 고를 수 있는 색이 20개가 되고, 같은 색이 Orca
사이드바 행에도 나타난다. 여기에 원본 병합을 견디기 위한 정비와 패키징 사고 예방이 함께
들어간다.

### 워크트리 색 직접 지정
- 워크트리 우클릭 → **"유니티 툴바 색"**: **자동**(기존 해시 배정), **팔레트 20색**,
  **직접 선택**(컬러 피커). 피커는 고른 색이 툴바에서 실제로 어떤 띠가 되는지 함께 보여준다.
- **막히는 색은 다른 워크트리가 직접 고른 색뿐이다**("(사용 중)" 표시 + 비활성, 커스텀도
  같으면 적용 잠김). 자동으로 배정된 색은 그냥 가져올 수 있고, 그 색을 쓰던 워크트리는
  다른 색으로 다시 배정된다. 자동색까지 막으면 워크트리 수가 색 수에 가까운 리포에서
  메뉴 전체가 "사용 중"으로 잠겨 아무것도 못 고르게 된다.
- 선택 즉시 반영: `unity:applyWorktreeTint` IPC가 유니티를 열지 않고 스크립트를 다시
  쓴다(유니티 프로젝트가 아니면 아무것도 만들지 않는 가드). 켜져 있는 에디터는 다음
  포커스의 스크립트 리로드에서 받는다. 그 선택으로 자동색이 밀린 형제 워크트리도 함께
  다시 쓰되 **바뀐 것만** 쓴다(불필요한 도메인 리로드 방지). 로컬 워크트리만 대상 —
  쓰기는 이 머신 파일시스템에서 일어난다.
- 저장은 리포별 `Repo.unityTintOverrides`(워크트리 **폴더명** → hex). 자동 배정과 같은
  기준이라 색이 폴더를 따라간다.
- 색 계산은 `src/shared/unity-worktree-tint-palette.ts` 한 곳뿐이다. 메뉴가 형제의 실효
  색을 예측해야 하는데, 구현이 둘이면 메뉴 표시와 실제 스크립트가 갈라진다. 규칙:
  살아 있는 형제의 수동 지정색은 예약되고, 삭제된 워크트리가 남긴 오버라이드는 아무것도
  예약하지 않는다. **워크트리가 색보다 많으면 어딘가는 겹친다** — 그때 겹치는 쪽은
  자동끼리이고, 수동 지정색은 침범하지 않는다. 단, 20색이 전부 수동 지정이면 양보할
  자동색이 남지 않아 그때는 자동 쪽이 수동색에 앉는다.
- codex 4라운드 검수 반영: `__proto__` 폴더명에서 오버라이드가 조용히 유실되던 것,
  유령 오버라이드가 팔레트를 고갈시키던 것, 포화 시 수동색 침범(자동끼리 공유를 우선하도록 — 위 단서 참고), 원격 경로가 로컬
  writer에 도달하던 것, basename 로직 중복, 스타일가이드를 어긴 Cancel 버튼.

### 색 20개, 두 단계
- 기존 10색에 10색을 더했다. 더한 10색의 **색상(Hue)은 기존 10색 사이의 중간값**에
  놓았다 — 툴바 띠는 고른 색의 채도·명도를 버리고 **색상만** 가져다 고정된 어두운 값으로
  칠하므로, 색상이 기존 색과 가까운 색을 아무렇게나 10개 더 넣으면 툴바에서는 앞의 10색과
  똑같아 보인다.
- **자동 배정은 그대로다.** 해시는 여전히 1단계 10색의 길이로 나누므로 **이미 있는
  워크트리의 색은 바뀌지 않고**, 2단계 색은 1단계가 전부 찬 뒤에야 배정된다. 두 단계를
  한 배열로 합치지 않은 이유가 이것이다 — 합치면 기존 워크트리 색이 전부 다시 굴러간다.
- 메뉴에는 20색이 모두 나온다(2단계는 "추가 색상" 아래). 배정 순서만 두 단계일 뿐,
  손으로 고를 때는 어느 쪽이든 똑같이 고를 수 있다.

### Orca 사이드바에 같은 색 표시
- 프로젝트 메뉴 → **"사이드바 유니티 색 표시"**: **꺼짐 / 왼쪽 색 바 / 행 배경 색 /
  오른쪽 색 칩**. 한 가지로 정하지 않은 이유는 사이드바 폭에서 셋이 서로 꽤 다르게
  읽히기 때문이다.
- **기본값은 "왼쪽 색 바"**. 기본이 켜짐이므로 **유니티 프로젝트로 확인된 리포에만**
  적용한다 — 평범한 git 리포의 행은 그대로 아무 색이 없다. 판정은 리포당 세션 1회
  파일시스템 조회이고, 답이 오기 전에는 추측하지 않고 아무것도 표시하지 않는다.
- 메뉴에서 **옵션 위에 마우스를 올리면 실제 행에 그 모양이 바로 미리보기**되고, 벗어나면
  저장된 설정으로 돌아간다. 미리보기는 저장도 IPC 전송도 하지 않는다.
- 세 모양 모두 레이아웃 흐름 밖이라, 색이 있든 없든 행의 글자 위치와 이름 잘림이 똑같다.
- 기본 체크아웃 행은 색이 없다 — 유니티 툴바 쪽 규칙과 같다.

### 원본 병합을 견디기 위한 정비
- **`pnpm test:fork`** 하나로 포크 기능 전용 테스트와 포크 케이스를 얹은 원본 테스트만
  돌린다(`config/fork-contract-tests.mjs`가 목록). 원본 병합 후 포크가 살아 있는지 보는
  빠른 관문이고, 병합이 목록의 파일을 지우거나 이름을 바꾸면 매니페스트 메타 테스트가
  즉시 실패한다 — 계약이 조용히 줄어들지 않는다.
- 원본이 크게 고치는 파일에 얹혀 있던 포크 코드를 포크 소유 모듈로 떼어냈다: 유니티 메뉴
  (`worktree-unity-menu.tsx`, 색 메뉴는 다시 `worktree-unity-tint-menu.tsx`), 포크 UI
  IPC 리스너(`fork-ui-ipc-listeners.ts`), 콜드 복원 resume 북키핑
  (`cold-restore-resume-bookkeeping.ts`). 원본 파일(`WorktreeContextMenu.tsx`,
  `useIpcEvents.ts`, `pty-connection.ts`)은 이제 import 한 줄과 호출 한 줄로만 포크에
  닿는다. 동작은 그대로이며 각 단계마다 codex 평가 PASS.

### 패키징 — `.claude/`를 app.asar에서 제외
- `.claude/worktrees/`에는 에이전트가 작업 중인 저장소 사본이 통째로 들어 있어
  (관측: +76MB) 용량부터 문제지만, 더 중요한 건 **빌드 도중에도 내용이 바뀌는 경로**라는
  점이다. mac 빌드는 한 번의 파일 스캔으로 x64와 arm64를 연달아 패킹하므로, 그 사이에
  크기가 변한 파일 하나가 뒤쪽 asar 오프셋을 전부 밀어 arm64 앱이 로그 한 줄 없이 죽는다.
  그 사고의 후보였다.

### mode-B E2E 하네스의 판정 기준
- follow 전환 E2E(`config/scripts/mode-b-follow-e2e.mjs`)가 **자기가 띄운 탭의 id**로
  훅을 조회한다. 전에는 에이전트 이름으로 골랐기 때문에, 사람이 열어 둔 같은 에이전트
  팬이 옆에 있으면 그쪽 훅을 읽고 멀쩡한 전환을 실패로 판정할 수 있었다. 탭 정리도
  하네스가 만든 탭 원장에 근거해서만 닫는다.

### 문서 정정 — "에이전트에게 알리기"
- machamy.2의 "알려진 한계"에 오래 남아 있던 **"토스트만 띄운다"는 서술이 틀렸다**는 것을
  이 판에서 바로잡았다. 실제로는 전환 후 6·15·30·60·90초에 걸쳐, 깨어난 에이전트 팬마다
  한 번씩 바뀐 경로와 브랜치를 PTY에 직접 써 넣는다. **팬에 도착하는 것까지만 보장**되고
  전송은 아니다 — 문구 끝에 Enter를 붙이지만 TUI가 그걸 받지 않고 입력창에 문구만 남는
  경우가 있고, 그때는 사용자가 직접 Enter를 눌러야 한다. 자세한 내용은 machamy.2의
  "알려진 한계".

## machamy.6 — 원본 `1.4.178-rc.2` 기준 · 2026-08-20

### 추가 — 유니티 워크트리별 색 (macOS)
- 리포 ⋯ 메뉴의 **"유니티 워크트리마다 다른 색 표시"** 토글(**기본 켜짐**, 끄려면 체크 해제).
  워크트리마다
  **플레이/일시정지가 있는 툴바 배경**이 고유 색으로 물들고, **창 제목 앞에 워크트리 이름**이
  붙는다 — 에디터 두 개를 동시에 열어도 한눈에 구분된다.
- **기본 워크트리는 무색**(Unity 기본 회색)이라 "색이 있으면 사이드 워크트리"로 읽힌다.
  기본 판정은 리포 경로 기준이라 스왑 후에도 유지된다.
- 색은 Catppuccin Mocha 10색. 폴더 이름 해시가 시작 슬롯이고, **형제 워크트리와 겹치면
  빈 색으로 밀어** 배정한다(정렬 순 배정이라 새 워크트리가 생겨도 기존 색은 그대로).
  실측: 워크트리 10개 전부 다른 색.
- 구현 메모: Unity엔 프로젝트별 테마 훅이 없다(테마 설정은 Light/Dark뿐, 색 설정은 사용자
  전역). 그래서 생성한 에디터 스크립트가 툴바의 UI Toolkit 트리에 **인라인 스타일**을
  넣는다(인라인이 USS를 이긴다). 내부 멤버 리플렉션 없이 창 타입 이름만 매칭하고, 실패하면
  조용히 기본 색으로 남는다. 파스텔을 그대로 칠하면 아이콘이 안 보여서, 그 색의 **색상(hue)만**
  가져다 고정된 어두운 채도·명도로 칠한다 — 회색에 섞는 방식은 워크트리가 늘수록 띠가 밝아져
  다크 테마를 깨고 밝은 아이콘을 가렸다.
- 스크립트는 `Assets/Private/`(대상 리포에서 이미 gitignore된 경로)에 들어간다. 기본 켜짐이라
  "그 리포가 마침 무시하고 있다"에 기댈 수 없어, **쓰기 전에 git에게 무시되는 경로인지 묻고
  (check-ignore) 아니면 그 리포는 통째로 건너뛴다** — 추적될 파일은 절대 만들지 않는다.
  토글을 끄면 다음 시드/열기 때 제거된다.
- 검증: 실제 에디터(6000.3.16f1) 배치 컴파일 에러 0(워크트리 2곳) + 툴바에 색이 입혀진
  스크린샷 + 창 제목 접두어 확인. 문서가 틀렸던 부분(`MainToolbarLabel`이 VisualElement라던
  것)과 등록만 되고 렌더되지 않던 툴바 칩은 실기 검증으로 걸러내 제거했다.

## machamy.5 — 원본 `1.4.178-rc.2` 기준 · 2026-08-20

### 수정 — 시드/신규 워크트리 첫 실행 유니티 크래시 ("두 번 열어야 됨")
- Firebase 플러그인이 에디터 로드마다 `google-services-desktop.json`을 mtime 비교로
  재생성하는데, 그 `AssetDatabase.CopyAsset`이 첫 실행 임포트 폭풍 속에서 Temp 이동
  실패 → 재시도 창 → 유니티(6000.3.16f1) 자체 크래시로 이어졌다(실기 2회 관측,
  두 번째 실행은 항상 성공). 생성 파일이 소스와 바이트 동일하면 mtime을 소스+2초로
  박아 게이트가 "최신"으로 읽게 함 — 시드 직후 + orca발 실행 직전 자동 수행,
  내용이 다르면 건드리지 않는다(정당한 재생성 보존).

### 추가 — 드래그로 기본 워크트리 전환 복원
- 머지 때 보류했던 드래그 경로를 새 포인터 드래그 시스템에 재배선. 자격이 맞는
  드래그 중에만 기본 행에 "Make default" 존이 나타나고, 링과 드롭이 같은 자격
  게이트를 공유한다. 존 위에서 놓을 때만 커밋(근접 폴백 제외), 링은 상태 기반이라
  커밋과 항상 일치.

## machamy.4 — 원본 `1.4.178-rc.2` 기준 · 2026-08-21

### 추가 — "Rider에서 열기" (유니티 메뉴)
- Rider가 설치돼 있으면(macOS, /Applications 또는 Toolbox 위치) 유니티 메뉴에 항목이
  뜬다. gitignore된 `.sln`이 워크트리에 없으면 기본 체크아웃의 `.sln`/`.csproj`를
  복사(워크트리 폴더명으로 개명)한 뒤 솔루션으로 연다 — 폴더 열기와 달리 Unity 연동
  (플레이·로그·디버거)이 산다. Library 시드도 같은 파일들을 함께 실어 나른다.
  둘 다 없으면 폴더로 열고 안내 토스트.
- 안전장치(codex 교차검수 반영): 복사는 절대 덮어쓰지 않음(COPYFILE_EXCL — 워크트리
  자체 csproj 보존), csproj 먼저·sln 마지막이라 부분 실패가 다음 시도에서 자가 복구,
  워크트리당 복사 1개 직렬화, sln 선택 결정적(폴더명 우선 후 정렬).

## machamy.3 — 원본 `1.4.178-rc.2` 병합 · 2026-08-19

원본 564커밋(대규모 모듈 재구조화 포함)을 병합하고, 그 위에 유니티 워크트리 지원과
마크다운 GitHub 스타일 보기를 얹은 라운드. 무엇을 이식하고 무엇을 버렸는지는 머지
원장으로 관리했고, 이식 결과는 codex 병렬 검수를 거쳤다.

### 추가 — 유니티 워크트리 지원 (macOS)
- **워크트리 우클릭 → "유니티용 Library 복사" / "Unity에서 열기".** 복사는 APFS
  Copy-on-Write(`cp -c`)라 수 초에 끝나고 추가 용량이 거의 없다. 스테이징 폴더에
  받아 원자적 rename으로 완성하므로 중간에 죽어도 반쪽 Library가 남지 않는다.
- **켜진 에디터의 Library는 절대 복사하지 않는다.** lockfile + 프로세스 테이블의
  `-projectpath`(경계 고정 정규식) + 최근 실행 TTL 3중 게이트. 세션 결합 항목
  (Temp 등)은 복사본에서 제거한다.
- **처음 열 때 복사 여부를 묻고**, 리포 ⋯ 메뉴의 "새 워크트리에 자동 복사" 토글로
  워크트리 생성 시 자동 복사도 가능하다.
- 실기 검증 중 유니티가 스왑 도중 Temp 경로 참조로 fatal을 낸 사례를 계기로,
  라이브 프로세스 판정과 원자적 스테이징을 하드닝했다.

### 추가 — 마크다운 GitHub 스타일 보기
- 원시 HTML이 섞여 프리뷰가 막는 .md에서 안내 배너의 버튼으로 GitHub-flavored
  렌더링을 볼 수 있다(포크 전용 기능임을 배너에 표기).

### 병합 — 원본 `1.4.178-rc.2` (564커밋, 충돌 36파일)
- 원본이 `shared/types.ts`·`persistence.ts`·worktrees 슬라이스·WorktreeList를 전부
  모듈로 쪼갠 재구조화를 수용하고, 포크 기능을 새 구조에 재이식했다:
  - 스왑 재키 머신 → `persistence/tracking-repos/worktree-identity-migration.ts`
    (재키 충돌 병합 `mergeReKeyedValue`, 경로 remap, priorWorktreeIds 캡 포함)
  - 마커 CAS → `store/slices/worktrees/session/default-switch-claim.ts`
  - 스위치 다이얼로그 UI → `use-default-worktree-switch-dialog.ts` 훅 +
    새 worktree-list 모듈(뷰포트/행/그룹핑)에 프롭 배선
  - 기본 행 판정(리포 경로 기준)을 새 가시성/정렬 모듈에 재적용
- **절전 신호를 `retainSurface`로 분리.** 원본이 일반 kill에도 `keepHistory`를 쓰게
  되어 의미가 충돌 — 포크의 "팬 유지" 신호를 별도 플래그로 갈라 원본 동작을 보존.
- 드래그로 Default 카드에 드롭하는 경로는 원본의 새 드래그 시스템과 미결합(후속) —
  컨텍스트 메뉴와 CLI 경로는 동작.
- 검증: typecheck 0 · lint 0 · 대상 스위트(훅 107 / main-ipc 314 / 사이드바+스토어
  279 / 마이그레이션 15) green, 전체 스위트로 최종 확인.

## machamy.2 — 원본 `1.4.176-rc.1` 기준 · 2026-08-09

기본 워크트리 전환의 **상태 보존**을 실기 계측으로 파고들어 고친 라운드. 수용 기준은
[`docs/reference/default-worktree-switch-acceptance.md`](docs/reference/default-worktree-switch-acceptance.md)에
"반드시 일어나야 하는 일 / 절대 일어나선 안 되는 일"로 정리했다.

### 수정 — 전환 시 상태 유실 (전부 실기에서 관측 후 수정)
- **탭이 통째로 사라지던 것.** sleep이 죽인 PTY의 exit이 억제되지 않아, 마지막 팬이
  닫히면 탭까지 닫혔다(`tab_close {reason:"pty-exit"}`). 스왑 전용 teardown 창(90초)을
  두어 억제한다. 이 창은 spawn 가드보다 오래 산다 — kill이 wake 이후에 도착하기 때문.
- **탭이 복제되던 것.** wake의 generic 재개가 새 탭을 만드는데, 스왑 직후엔 탭 목록이
  정착 전이라 살아있는 탭도 "없음"으로 읽혀 레코드마다 포크가 생겼다(4탭 → 7탭,
  클로드·코덱스가 각 3개). follow 전환에서 **새 탭 생성을 제거**했다.
- **분할 팬이 죽은 세션에 reattach**하던 것. 스왑을 왕복하면 죽은 id가 소유권 검사를
  통과해 빈 팬으로 남았다. 리마운트 전에 죽은 바인딩을 지운다.
- **팬이 PTY 없이 방치**되던 것. wake 후 4·12·25·45·75초에 재기동 스윕을 돌린다.
- **사이드바에서 에이전트가 터미널 행으로 강등**되던 것. 두 원인이 겹쳤다 — 전환이
  `launchAgent`를 벗겨냈고(팬이 잠깐 셸로 보이면 "에이전트 종료"로 오판), 재개된
  에이전트에 상태 행이 없었다. 전자는 teardown 창으로 막고, 후자는 콜드 복원 **스폰
  성공 후** 상태 행을 심는다(스폰 전에 심으면 콜드 복원이 resume을 건너뛴다).
- **재시작 후 전환이 영구 거부**되던 것. 콜드 복원 에이전트는 다음 턴까지 훅을 안 뿜어
  "캡처 불가"로 남았다. resume identity를 재기록하고, readiness gate는 거부 대신
  경고 후 진행한다.
- **한글 워크트리 2개의 Claude 기록 유실.** Claude는 폴더명을 `[^a-zA-Z0-9]` → `-`로
  접으므로 같은 부모 아래 글자 수가 같은 한글 이름 둘은 충돌한다. 충돌 시 교환을
  건너뛰고, 3단계 rename이 실패하면 원위치로 되돌린다.

### 추가
- **미추적 파일 이동 여부 선택.** 기본은 브랜치를 따라 이동(기존 동작), 팝업 체크박스
  "추적되지 않은 파일은 제자리에 두기"로 끌 수 있다. CLI `--keep-untracked-in-place`.
  RPC `includeUntracked`는 선택적 필드라 구버전 클라이언트는 영향 없다.
- 전환 팝업 문구가 미추적 파일의 실제 거동을 체크박스 상태에 맞춰 표시한다.

### 검증 상태
- 실기 3왕복 + 2왕복: **탭 집합 교환 · 탭 증가 없음 · 세션 동일 · 사이드바 에이전트 행**
  전부 통과. 초기 검증이 "세션 id 일치"만 봐서 탭 포크를 놓쳤던 것을 계기로,
  **탭 개수와 사이드바 행**을 판정에 포함시켰다.
- 한글 경로·브랜치·미추적 파일 스왑은 실제 git 저장소로 검증(회귀 테스트 포함).
- **임시 계측 유지 중**: `mode_b_*`, `pane_exit_teardown`, `tab_close`,
  `orphan_sweep_*`, `mode_b_pane_connect` breadcrumb이 `main.trace.ndjson`에 남는다.
  이번 결함들을 전부 이 계측으로 특정했으므로 기능이 안정될 때까지 유지한다.
- **미구현(이 시점 기준)**: "에이전트에게 알리기"는 토스트만 띄웠다 — 이후 해소됐다.
  아래 "알려진 한계" 참고.

### 알려진 한계
- ~~**"에이전트에게 알리기" 토글은 에이전트에게 알리지 않는다.** 전환 결과를 토스트로
  보여줄 뿐이며, 살아있는 TUI 입력창에 자동 주입하는 기능은 없다.~~
  **정정**: 이 한계는 machamy.2 시점에는 사실이었으나 `b4270893`(2026-08-14)에서
  해소됐고, 그 뒤로도 여기 그대로 남아 잘못된 채로 인용돼 왔다. 지금은 전환 후
  6·15·30·60·90초에 걸쳐, 깨어난 에이전트 팬마다 한 번씩 바뀐 경로와 브랜치를 PTY에
  직접 써 넣는다(턴이 진행 중인 팬은 건드리지 않는다). 다만 **팬에 도착하는 것은
  보장되지만 전송까지는 보장되지 않는다** — 문구 끝에 Enter(`\r`)를 붙이지만 에이전트
  TUI가 그걸 받지 않고 입력창에 문구만 남는 경우가 관측됐고, 그때는 사용자가 직접
  Enter를 눌러야 한다.
- 플레인 셸은 스크롤백이 유실된다(프로세스 이동 불가). 탭·위치·분할은 유지된다.
- 한 번도 대화하지 않은 에이전트는 보존할 세션이 없어 새 세션으로 시작한다.

---

## machamy.1 — 원본 `1.4.169-rc.0` (`5f187e083`) 기준 · 2026-08-06

로컬 빌드 예: `1.4.169-rc.0.machamy.1.local.<ts>.cd3aacff7`

### 추가
- **기본 워크트리 전환(Make Default Worktree).** 서브 워크트리를 리포의 기본
  체크아웃으로 승격한다. **두 워크트리의 브랜치를 제자리에서 맞바꾼다** — 선택한
  브랜치가 리포 경로에, 옛 기본 브랜치가 선택 워크트리에 체크아웃된다. 폴더는 이동하지
  않으므로 git의 main worktree(`.git` 보유자)가 **리포 경로에 고정**되고, GitHub
  Desktop을 리포 폴더에서 열면 승격된 브랜치가 보인다. 커밋 안 된·스테이징된·untracked
  변경은 브랜치를 따라가고, ignored 파일은 제자리에 남는다. 워크트리를 **Default**
  카드에 드롭하거나 `orca worktree default set --worktree <선택자>`.
- **"에이전트가 브랜치를 따라가기" 토글** (기본 꺼짐). 켜면 양쪽 워크트리를 절전하고,
  절전 세션 콘텐츠 + Claude 트랜스크립트를 맞교환한 뒤, 각 에이전트를 자기 브랜치가 이제
  있는 워크트리에서 재개한다. git main은 여전히 리포 경로 고정, 재개 가능한
  에이전트(Claude·Codex)만 따라간다.
- **"에이전트에게 바뀐 내용 알리기" 토글** (기본 꺼짐). 전환 후 어느 워크트리가 어느
  브랜치가 됐는지 정확히 표시한다.

### 변경
- 사이드바의 **기본/primary** 신호 — 별 배지, 리포 그룹 정렬 앵커, Cmd+J 칩,
  "기본 브랜치 숨김" 필터, "슬립 숨김" 예외, 삭제/프로젝트 제거 가드 — 를 git의
  `isMainWorktree`가 아니라 **리포 경로** 기준으로 판정한다. 전환 후 "기본"이 두 개로
  뜨거나 별이 엉뚱한 행에 붙던 문제를 고쳤다.

### 수정
- **브랜치 스왑 데이터 안전성** (실제 git으로 적대적 테스트하며 발견):
  - 두 번째 stash 캡처가 실패해도(예: 한쪽이 병합 진행 중) 첫 워크트리의 커밋 안 된
    작업이 dangling stash로 사라지지 않도록 복원한다.
  - post-checkout 훅이 non-zero로 끝나도 워크트리가 detached로 방치되거나 가짜
    `recovery_required`가 뜨지 않게, 실제 HEAD 상태로 판정한다.
  - merge/cherry-pick/revert 진행 중 워크트리는 사전에 명확한 메시지로 거부한다.
- **패키징 daemon-entry 부팅**이 패키징 직후의 일시적 `dlopen` 경합(`ETIMEDOUT`)에
  전체 실패 대신 재시도한다.
- **"에이전트 따라가기" 실사용 3연타 수정** (실기 리포트로 발견):
  - 전환 후 활성 워크스페이스가 없어서 에이전트가 숨은 백그라운드 탭으로 포크되던 문제 —
    승격된 기본을 활성화한 뒤 wake한다.
  - `git switch --detach`가 만들던 detached HEAD 순간에 JetBrains(Rider)가 캐시를
    버리던 문제 — 임시 park 브랜치로 HEAD를 항상 symbolic으로 유지한다.
  - **근본 원인: 터미널이 이동 대신 닫히던 문제.** 데몬 세션 id에 생성 시점 워크트리
    id가 박혀 있는데, 콘텐츠 스왑 후 소유권 검사가 이 id를 거부하고는 죽은 세션에
    bare-attach → 빈 팬 → 탭 자동 닫힘. 이제 소유권 불일치 + 절전 레코드 보유 팬은
    낡은 바인딩을 지우고 **새 경로에서 cold-restore `--resume`** 로 재개한다. 살아있는
    탭의 `startupCwd`도 새 집으로 리맵. follow 전환 시 양쪽 워크트리의 절전 에이전트
    탭(대기 중 포함)을 전부 마운트해 눈에 보이게 복원한다. (21-에이전트 적대 검증 +
    스토어 시뮬레이션 테스트로 원인 4중 확인)

### 커밋

> 이력을 한 커밋으로 압축하면서 아래 해시는 모두 그 단일 커밋(`b4270893`)을 가리킨다.
> 원래는 커밋별 해시였지만, 비공개 저장소의 실제 프로젝트·브랜치 이름이 커밋 메시지와
> 테스트 픽스처에 섞여 들어가 있어 포크 커밋 88개를 다시 썼다. 요약 열은 당시 커밋
> 제목 그대로이므로 무엇이 언제 들어갔는지는 여전히 읽을 수 있다.

| 해시 | 요약 |
|------|------|
| `b4270893` | feat(worktree): default-worktree switch with agent-safe hardening |
| `b4270893` | fix(build): retry packaged daemon-entry boot on post-pack dlopen contention |
| `b4270893` | fix(sidebar): key default-workspace UI on the repo path, not git isMainWorktree |
| `b4270893` | refactor(worktree): make default switch a branch swap, keeping git main in place |
| `b4270893` | fix(worktree): harden branch swap against data loss and stuck-state edge cases |
| `b4270893` | feat(worktree): add "agents follow" and "notify" toggles to default switch |
| `b4270893` | fix(worktree): surface followed agents and avoid detached HEAD on switch |
| `b4270893` | fix(worktree): resume followed agents after a default switch instead of closing their tabs |
| `b4270893` | fix(worktree): re-seed follow-switch sleeping records lost to swap-window churn |
| `b4270893` | fix(worktree): stop mounted panes from consuming follow-switch records at sleep |
| `b4270893` | fix(worktree): re-home open editor tabs when agents follow a default switch |
| `b4270893` | fix(worktree): carry every workspace window through the follow switch |
| `b4270893` | fix(worktree): codex follow support + remaining per-workspace state remaps |
| `b4270893` | feat(worktree): drive the full default-switch flow from the CLI (--follow-agents --ui-flow) |
| `b4270893` | fix(cli): register the default-switch booleans so --follow-agents actually transmits |
| `b4270893` | test(worktree): mode-B follow-switch E2E harness driving the live app via CLI |
| `b4270893` | fix(worktree): show followed agents in the sidebar immediately after a switch |
| `b4270893` | feat(worktree): show a moving-agents indicator during the default switch |

### 검증 상태
- **E2E 실기 검증 완료** (`config/scripts/mode-b-follow-e2e.mjs` — 실행 중인 앱을
  CLI로 구동): 클로드+코덱스 동시, **같은 provider 세션으로 왕복 복원**을 연속
  5회전(스왑 10회) 전부 통과. 사이드바 칩은 훅 도착 전에도 즉시 표시(절전 레코드
  시딩), 전환 중엔 "Moving agents…" 스피너 행 표시.
- **실기 확인됨**: 에디터 파일 로드, Rider 캐시 유지, 사이드바 표시(육안).
- **검증됨** (테스트/시뮬레이션): 창 전수 보존 — 터미널 탭·스플릿, 에디터 열린 파일·
  더티 버퍼, 브라우저 탭, 파일 트리 펼침, 닫은 탭 재열기, 검색 패널, 뒤로가기 히스토리,
  diff 탭 — 재시작 후에도 유지(main 영속 리맵). 절전 중 팬의 즉발 재기동 차단(가드),
  레코드 소실 시 스냅샷 재시드.
- **알려진 잔여**: 플레인 셸 터미널 탭은 스왑 후 새로 열림(셸 프로세스는 이동 불가),
  combined-diff/충돌 리뷰 탭은 리키 제외(재열기 필요), 스왑 중 지연저장 극소 경쟁 창.
- **미구현(이 시점 기준)**: "알림" 노트를 실행 중 TUI 에이전트 입력창에 자동 주입
  (당시엔 안전·검증 가능한 방법이 없었음). `b4270893`(2026-08-14)에서 구현됐다 —
  범위와 남은 한계는 machamy.2의 "알려진 한계" 참고.
- **임시 계측**: `mode_b_*`/`sleeping_record_delete`/`worktree_purge` breadcrumb이
  `main.trace.ndjson`에 남는다 — 실기 확인 완료 후 제거 예정.
